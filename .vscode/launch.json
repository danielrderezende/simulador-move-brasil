{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Abrir index.html no navegador",
            "type": "pwa-chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}